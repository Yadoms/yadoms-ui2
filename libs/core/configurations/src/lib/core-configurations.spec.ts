import { coreConfigurations } from './core-configurations';

describe('coreConfigurations', () => {
  it('should work', () => {
    expect(coreConfigurations()).toEqual('core-configurations');
  });
});
